{
  "$schema": "http://json-schema.org/schema",
  "$id": "configurable-module",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "the name of module",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "what name would you like to use for this module?"
    }
  },
  "required": ["name"]
}
